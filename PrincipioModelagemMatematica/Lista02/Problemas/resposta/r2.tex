Tomando logaritmo em ambos os lados de
\[
y = m x^b,
\]
obtemos
\[
\log y = \log m + b\log x.
\]
Se definirmos $Y=\log y$ e $X=\log x$, segue que
\[
Y = bX + \log m,
\]
que e a equacao de uma reta no grafico log-log. Portanto, a inclinacao e $b$ e o intercepto e $\log m$.

