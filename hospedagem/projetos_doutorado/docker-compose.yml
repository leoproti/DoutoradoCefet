services:
  backend:
    build:
      context: ./backend
    container_name: doutorado-backend
    restart: unless-stopped
    ports:
      - "8000:8000"

  frontend:
    build:
      context: ./frontend
    container_name: doutorado-frontend
    restart: unless-stopped
    depends_on:
      - backend
    environment:
      - BACKEND_INTERNAL_URL=http://backend:8000
    ports:
      - "3000:3000"
